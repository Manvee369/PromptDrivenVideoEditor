fastapi
uvicorn[standard]
starlette
pydantic
pydantic-settings
python-multipart

# Video/Audio processing
ffmpeg-python
opencv-python
librosa
soundfile
scipy
numpy
scikit-learn
soxr

# Scene detection
scenedetect[opencv]

# Face detection
mediapipe

# Image processing (thumbnails)
Pillow

# Transcription — faster-whisper at fp32 (identical accuracy to openai-whisper, ~2-4x faster on CPU)
faster-whisper
# WhisperX adds wav2vec2 forced alignment for tighter word boundaries.
# Optional at runtime — transcribe.py falls back if it fails to import.
whisperx

# Visual understanding (SigLIP)
open-clip-torch
torch
torchvision
transformers

# HTTP
requests

# Job queue + cross-process safety
redis
rq
filelock

# ─── Azure SDK (optional — only needed when PDVE_STORAGE_BACKEND=azure
#                          or PDVE_AZURE_SPEECH_KEY is set) ───────────────
# Uncomment to install, or install selectively:
#   pip install azure-storage-blob azure-cognitiveservices-speech
# azure-storage-blob
# azure-cognitiveservices-speech
