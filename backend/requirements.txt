fastapi
uvicorn[standard]
starlette
pydantic
pydantic-settings
python-multipart

# Video/Audio processing
ffmpeg-python
opencv-python
librosa
soundfile
scipy
numpy
scikit-learn
soxr

# Scene detection
scenedetect[opencv]

# Face detection
mediapipe

# Image processing (thumbnails)
Pillow

# Transcription — faster-whisper at fp32 (identical accuracy to openai-whisper, ~2-4x faster on CPU)
faster-whisper
# WhisperX adds wav2vec2 forced alignment for tighter word boundaries.
# Optional at runtime — transcribe.py falls back if it fails to import.
whisperx

# Visual understanding (SigLIP)
open-clip-torch
torch
torchvision
transformers

# HTTP
requests

# Job queue + cross-process safety
redis
rq
filelock
